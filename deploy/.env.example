# Copy this file to .env and fill in the values
# NEVER commit .env to git

# PostgreSQL password (make this a long random string)
DB_PASSWORD=change_me_to_something_random

# Session secret (make this a long random string, 32+ characters)
SESSION_SECRET=change_me_to_a_long_random_secret_string

# App login credentials
APP_USERNAME=blending
APP_PASSWORD=dallas2026
