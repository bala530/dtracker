kind = "web"
previewPath = "/"
title = "Bug Tracker"
version = "1.0.0"
id = "artifacts/bug-tracker"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24577

[services.development]
run = "pnpm --filter @workspace/bug-tracker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bug-tracker", "run", "build" ]
publicDir = "artifacts/bug-tracker/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24577"
BASE_PATH = "/"
